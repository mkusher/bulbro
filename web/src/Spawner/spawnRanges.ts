import type { Range } from "@/geometry";

export const shortRange: Range =
	{
		from: 200,
		to: 400,
	};
export const mediumRange: Range =
	{
		from: 400,
		to: 600,
	};
export const longRange: Range =
	{
		from: 600,
		to: 900,
	};
