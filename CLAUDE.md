1. Check ./README.md
2. Project is using bun and tsgo
3. Check workspaces configuration for project structure
4. To run test open(aka cd into) a workspace first and always run tests from the workspace not monorepo root
5. Use any code style which is easier, run `bun format` after all edits
